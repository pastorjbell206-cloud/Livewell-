# Robots.txt for LiveWell by James Bell

User-agent: *
Allow: /
# OG images are served from /api/og — social scrapers (Facebook, X) and
# Google Images respect robots.txt when fetching og:image, so this Allow
# must come before the /api disallow or every share card loses its image.
Allow: /api/og
Disallow: /admin
Disallow: /api
Disallow: /.env
Disallow: /node_modules

# AI answer engines and LLM crawlers are explicitly welcomed. When a bot
# matches a named block it uses ONLY that block, so these repeat the same
# rules as the wildcard above (crawl everything except admin and the API,
# but do read the OG card endpoint). A plain-language summary of the site
# for these engines lives at /llms.txt.
User-agent: GPTBot
User-agent: OAI-SearchBot
User-agent: ChatGPT-User
User-agent: ClaudeBot
User-agent: Claude-Web
User-agent: Claude-User
User-agent: anthropic-ai
User-agent: PerplexityBot
User-agent: Perplexity-User
User-agent: Google-Extended
User-agent: Applebot-Extended
User-agent: Amazonbot
User-agent: Meta-ExternalAgent
User-agent: cohere-ai
User-agent: DuckAssistBot
User-agent: CCBot
Allow: /
Allow: /api/og
Disallow: /admin
Disallow: /api

# Sitemap
Sitemap: https://www.livewellbyjamesbell.co/sitemap.xml
