# Pending rewrites

Rewrites that are written but not yet through the integrity review
(docs/rewrites/STANDARD.md). `scripts/apply-rewrites.mjs` reads only the files
directly in `content/rewrites/`, so nothing here reaches the site. After the
review, a file moves up one folder and the applier runs.
